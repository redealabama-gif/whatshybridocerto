# macOS AppleDouble / resource fork files. They previously ended up checked
# into this repo and broke build.js (which concatenates content/wpp-hooks-parts/*.js
# — the ._* siblings are binary metadata, not real JS, and produced
# "Invalid or unexpected token" at the page-world load).
._*
.DS_Store

# Build artifacts (regenerated by `node whatshybrid-extension/build.js`)
whatshybrid-extension/dist/build-report.json

node_modules/
